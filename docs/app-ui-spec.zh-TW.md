# 寫一個 catnip app

**語言：** [English](app-ui-spec.md) · 繁體中文

app 提供什麼、平台提供什麼、兩者在哪裡交會。文中描述的能力是目標設計，有些尚未實作；
頁尾的〈這份規格要求了什麼，以及每一項落在哪裡〉把最近這一輪的改動對應到程式碼。

## 畫布

320x240，橫向，深色主題。有兩種模式，app 不特別要求就是第一種。

**一般模式。** 平台畫的標頭，底下的主區域是你的。

```text
┌──────────────────────────────────────────────┐
│ [BATTERY]      [TITLE]           [PAGE]      │  標頭 — 平台的
├──────────────────────────────────────────────┤
│                                              │
│                  主區域                       │  你的
│                                              │
└──────────────────────────────────────────────┘
```

你提供兩樣東西：**一個標題**與**一棵內容樹**。frame 會在**可見**的那一頁外面畫標頭——包括你 push 出來的頁，所以對話框跟 root 一樣有框。

**全螢幕模式**（`frame: "bare"`）。沒有標頭，整片 320x240 都是 app 的，給真正是一塊畫布的東西——遊戲、時鐘面、訊號圖。
它換到的是**空間，不是座標**：規則 1 依然成立，你依然是建一棵沒有幾何的樹。

### 主區域放什麼

四種形狀，沒有第五種。四者都是 `list`，差別只在平台怎麼排。

| 形狀              | 什麼時候                         | 排法                           |
| ----------------- | -------------------------------- | ------------------------------ |
| **一個 icon**     | landing page——貓、開場畫面       | 置中，獨自一個                 |
| **最多六個 icon** | 一組可選的東西                   | **3x2 grid**，聚焦的那個加外框 |
| **文字列**        | 更長的、或需要文字說明的         | 一列一行                       |
| **數值**          | 設定——是量，不是選擇             | **直立 bar**，並排             |
| **一張臉**        | 一個拿來看的東西，周圍是它的標籤 | **canvas**——一個中心加兩條線   |

前三種回答的都是「哪一個？」。第四種之所以存在，是因為它們都答不了「多少？」——一個量沒有天然的列、也沒有天然的 icon，硬套一種形狀只會把數字擺到不是重點的位置。
它是 `ui.list{ layout = "mixer" }`：每個子節點帶 `value`(0–100，bar 填到哪)與 `value_text`(印在上面的讀數)，而**短按 A 把那一欄推到下一個值**。
高度就是數字，所以整頁可以互相比對；觸控目標也從一列文字變成一根與區域等高的柱子。

```text
    70%      30 s      5%     2.5 s
   ╔═══╗    ┌───┐    ┌───┐    ┌───┐
   ║███║    │   │    │   │    │███│
   ║███║    │███│    │███│    │███│
   ╚═══╝    └───┘    └───┘    └───┘
   Screen   Idle-off   LED    Breath
```

**一張臉**兩個問題都不回答:它是一個拿來看的東西,周圍圍著它的標籤,而時鐘正是這樣。
`ui.list{ layout = "canvas" }`——以及 `frame: "bare"` 給一張畫面的東西——是**繞著它的中心**排的:`display` 角色的那一個放在區域正中央,你在它之前命名的組成上面那條線、之後命名的組成下面那條,每一條都是第一個靠左、最後一個靠右。
**你命名它們的順序就是版面**,而你依然沒有說出任何一個的位置。

一個值是**幾個具名階的梯子**，不是連續區間，因為「下一個值」需要一份有限的清單。
挑階的時候要讓「最後一階再按一下」落在安全的地方：梯子是循環的。

超過六個是**下一頁**，不是更長的捲動。欄固定三、列固定二，第七個 icon 不會把上面六個重排成更緊的樣子——
一個在卡片變長時就變成四欄的 grid，會把使用者已經記住位置的每一個 icon 都搬走。
選取依然帶動：它一格一格走，走出這一頁時，頁在它底下翻過去。
grid 就是 `ui.list{ layout = "grid" }`——同一個 node、同一個 `selected` 整數、同一組事件，只有排列方向不同。

### 五種畫面

上面那幾種形狀說的是內容**是什麼**；這一節說的是它外面包著什麼 **chrome**——而 chrome 不是一件一件挑的。
整台裝置只有五種畫面，app 挑一種，其餘的就跟著決定了。

| 畫面             | 主區域         | 標頭   | 控制提示   | 動作列 |
| ---------------- | -------------- | ------ | ---------- | ------ |
| **1. 全螢幕**    | 任意           | 沒有   | 沒有       | 沒有   |
| **2. 吉祥物**    | 一個 icon      | 浮在上 | 選配，浮動 | 可浮動 |
| **3. Icon grid** | 3x2 icon，分頁 | 一定有 | 保留左下角 | 可浮動 |
| **4. 清單**      | 文字列         | 一定有 | 保留左下角 | 可浮動 |
| **5. 設定**      | 數值，分頁     | 一定有 | 保留左下角 | 可浮動 |

**1. 全螢幕**（`frame: "bare"`）。平台**什麼都不畫**——沒有 bar、沒有提示、沒有計數，一個像素都沒有——
只留下一個手勢：長按 B。這是唯一一個 app 獨自面對面板的畫面，而它換到的就只有這件事：
**沒有被保留的角落要閃**，因為根本沒有任何東西被保留。

**2. 吉祥物 / 單一 icon。** 一個 icon 置中，尺寸是對著**整個 240** 算的——內容不被 bar 往下推，
bar **浮在它上面**。bar 一定會畫。提示在這裡是選配的，也只有這裡是，因為置中的圖形無論如何都會把兩個下角空著。

**3. Icon grid。** 三欄兩列、分頁，如上。標頭在這裡不是選配：`2/3` 是螢幕上唯一一個說「還有第三頁」的東西，
少了它，一個 grid 看起來就是全部了。

**4. 清單。** 一項一列、一列一行，而且**區域高度切成整數列**。
底緣露出半列讀起來像繪製壞掉，而不像「還可以往下」的邀請；而且也不需要它來當邀請：
標頭的 `3/11` 已經說了還有更多——這也是標頭在這裡同樣不是選配的第二個理由。

**5. 設定。** 就是「**數值**」形狀，分頁——一頁放得下幾欄就放幾欄（欄寬要讀得清楚），下一螢幕是下一頁，不是往旁邊捲。
它是唯一一個有自己專屬手勢的畫面，見〈設定一個值〉。

### 兩個浮動 widget

有兩樣東西浮在主區域上面。兩樣都是平台的、都不是任何一棵樹碰得到的，也都畫在 top layer，所以 app 蓋不過去：

**控制提示**，左下角。它會不會讓 app 付出代價，是**逐畫面**回答的，而問題永遠是同一個：
**內容碰得到那個角落嗎？**

五種裡有三種碰得到，而它們就付這個代價：清單最底下那一列是整片寬的、設定頁最左邊那一欄是整個區域的高度、
grid 的左下角那一格就在那裡。這三種**保留**那一條，換到的是一個矮一點的區域。
吉祥物碰不到——置中的圖形是個中間，而為了一個會被畫在空白面板上的提示扣掉 34 px，
等於平台向自己沒在用的空間收租。全螢幕什麼都不保留，因為它上面根本什麼都不畫。

**規則是那個問題，不是那張清單**：以後多出來的形狀自己回答它。

**動作列**，沿著底部，有兩種形狀。

_兩個 icon_——一般情況，而且它**不是選單**。左邊那個**就是 A**、右邊那個**就是 B**：
這條列是使用者拇指底下那兩顆按鍵的**圖**，不是第三個要瞄準的東西。
預設那一對是 Yes / No。app 可以改寫肯定的那一個，也可以改寫 B 取消的是什麼；
B 永遠是否定的那一個，而平台**拒絕**把它綁到 manifest 標為破壞性的動作上。

_三個 icon_——modal，因為三個對不到兩顆按鍵。它升起的期間，這條列會把左右接走：
左右在三個之間走、A 執行聚焦的那個、B 把列收回去。上下仍然屬於底下的內容，而底下的內容一直看得見。

**形狀不是 app 挑的。** 一條列會變成 modal，剛好就是「它說不成兩顆按鍵」的時候，而這一條規則有兩種成立方式：
有三個，或者有兩個而第二個不能放在 B 上。**碰不到的動作不是動作**，所以右邊那個是破壞性的那一對，會改成用左右走。

兩種形狀都可以**點一下直接執行**。那是手指抵達 Cancel 的唯一路徑，也是這條列為什麼要畫出一個看得見的取消。

**兩個同時在場時，提示騎在動作列上面。**
動作列從底緣升起，而且兩個或三個 icon 都需要整片寬度，所以它不會從任何東西的右邊開始；
是提示被抬高，抬高的量剛好是動作列的高度，坐在它的左上肩上。
它依然是「除了動作列以外最低、最左」的那個東西——而「左下角」從來就只是這個意思，不是一個座標。

**而且它是替動作列回答的，因為現在在聽的是動作列。**
規則沒有變——**一個方向會亮，是因為擁有它的那個東西真的拿它做了什麼**——變的只有擁有者是誰。
兩 icon 形狀下，動作列根本不擁有任何方向（那兩個 icon **就是** A 和 B），
所以每一個箭頭仍然代表底下那一頁的意思，提示除了高度以外什麼都沒變。
三 icon 形狀下，動作列擁有左右，所以那兩個為「在三個之間走」而亮，上下則繼續替底下的內容回答。
**一個剛剛被塞了第三個選擇的使用者，正好就是需要被告知「左右現在有作用了」的那個人**——那就是這個提示存在的理由。

設了 `"hints": false` 的 app 在這裡一樣沒有提示。動作列不受那個旗標影響：它不是控制的說明，它本身就是控制之一。

**長按 B 是安全區，而且永遠不在這條列上。** 短按 B 是可以談的——app 可以接走，動作列也可以改綁。
長按 B 不論多深都回到那隻貓，而且不會抵達任何人的 handler。

**每一列都保留前導 icon 槽**，不管它有沒有 icon：glyph 14 px 加 6 px 間距，所以文字從第 20 px 開始。
是「一律」而不是「清單裡有人用到才留」——一條沒有條件的規則，而且某列後來加上 icon 時，文字不會整批位移。
**列必須有文字，icon 是選配。**

## 規則

1. **只命名，不擺放。** 說出你要顯示什麼——一個 label、一個 list、一個 icon。永遠不要求像素、座標、尺寸或角落。
2. **樹建一次，之後只寫屬性。** renderer 是 retained：它會保留為某個 node 造出來的物件，並用 table identity 比對子項。移動選取是寫一個屬性，不是重建整頁。
3. **一列就是 `[icon] [name]`，而且 icon 槽永遠都在。** 列是 `list` 的子項，可選取、可觸控。觸控事件送給**清單**，並帶上列索引。
   文字是必要的，icon 不是，而那個空間兩種情況都保留。
4. **`selected` 是你自己的。** 它是一個整數，而且只由上往下傳。平台把 `prev`/`next` post 給你聚焦中的清單，由你去動那個數字。
5. **用名字挑 icon，或自己附一個。** 內建 glyph(`folder`、`file`……)代表**類別**；你的 `icon.png` 是這個 app 的**身分**。
   不認得的 glyph 會安全退化，缺圖會退回吉祥物。內建那套是 catnip 自己的實心彩色 icon(`firmware/assets/icons`)，不是借來的符號字型。
6. **讓 `[PAGE]` 自己推導出來。** frame 依主區域放的東西自行判斷，沒有任何 API 能設定它：
   grid 數**頁**、清單數**列**、carousel 與 mixer 數**格**，單一 icon 什麼都不數、留白。
   grid 顯示 `2/3` 而不是 `8/14`，因為使用者在那裡看到的是**頁面上的位置**，不是序號。
7. **短按是主要動作，長按是次要動作——A 與 B 一致。** 短按 A 啟用；長按 A 給出「選取項目」的選項。
   app 做得到的事**永遠不出現在畫面上**——操作只存在於長按 A 之後。
8. **只有真的有得爬時才接走短按 B。** 處理掉了就從 `on_back` 回傳 truthy，沒處理就回傳 falsy 或什麼都不回。
9. **不要期待長按 B。** 那是回家，是平台的，不會抵達你任何一個 handler。電源鍵也一樣。
10. **manifest 宣告，Lua 決定。** manifest 擁有身分與外觀(`id`、`name`、`icon`);Lua 擁有行為與適用性。`id` 是兩者之間的接縫。
11. **空就是留白。** 不要列、也不要佔位字。留白是「沒有東西」的誠實呈現。

## 手勢

其中四個在整台裝置裡都是同一個意思，平台的畫面與 app 的畫面一視同仁。
一個 app 用這四個之中的任何一個表達別的意思，不是「不一樣」，是**錯**：

| 手勢       | 意思       | 也就是說                   |
| ---------- | ---------- | -------------------------- |
| **短按 A** | 選取       | 啟用目前聚焦的那個東西     |
| **短按 B** | 取消       | 放回去、往上一層、或離開   |
| **長按 A** | 更多選項   | 選取項目的動作列           |
| **長按 B** | 離開回到貓 | 平台的，任何深度，永遠有效 |

其餘的都取決於你在哪裡——這正是裝置要在角落畫一張圖說明它們的原因。

| 手勢         | 意義                                   | 抵達你的形式               |
| ------------ | -------------------------------------- | -------------------------- |
| Up / Down    | 移動選取；清單捲動跟隨。**按住會連發** | `prev` / `next` 給聚焦清單 |
| Left / Right | 在可聚焦元件之間移動焦點               | 不傳；焦點環由平台移動     |
| 短按 A       | 啟用目前項目——數值則是下一階           | `on_click(self)`           |
| 長按 A       | **選取項目**的選項                     | `on_options(self, index)`  |
| 點一列       | 選取並啟用該列                         | `on_click(self, index)`    |
| 長按一列     | 那一列的選項                           | `on_options(self, index)`  |
| 滑動         | 用手指做的搖桿四方向                   | 該方向本來的意義           |
| 短按 B       | 返回——pop、往上一層、或離開            | 可見頁的 `on_back(self)`   |
| 長按 B       | 回家——回到貓，不論多深                 | 不傳；平台自己留著         |
| 長按電源     | 關機                                   | 不傳；PMIC 自己留著        |

### 裝置會告訴你哪幾個方向真的有作用

左下角四個小箭頭，一個方向一個，**這裡有作用就亮，沒作用就暗**。

```text
        ▲          上      在貓上是亮的：它開啟全部 app 的 grid
      ◄   ►        左右    亮：翻 carousel
        ▼          下      亮：到裝置頁
```

它是**畫出來的，不是排字排出來的** —— 四個三角形從同一個圓心算出，亮的時候是土黃色、暗的時候是既有的 faint 灰。
四個字型 glyph 用手擺座標永遠喬不對稱，因為那幾個箭頭字符的寬高本來就不一樣；**共同的圓心**才是十字能正的唯一理由。
顏色刻意不用內文的白：這個提示不是文字，是你拇指底下那個實體控制的圖。

**變暗才是重點**，那也是這個提示存在的唯一理由。有不少方向會依所在位置而沒有作用 ——
只有兩顆按鈕的頁面「上下」沒有東西可移、ring 走到最後一格「右」也無處可去。
在一台沒有提示的機器上，**「這個方向沒作用」和「這台機器不理我了」看起來一模一樣**，而使用者會選擇相信後者。

**沒有任何人「宣告」它。** 答案是從樹推導出來的：focused node 有沒有對應那個方向會 post 的 handler，再加上平台自己的綁定。
**回答按鍵的是同一個函式**，所以不可能出現「箭頭亮著但按下去沒反應」——
一個頁面多了 `on_next`，它的箭頭就自己亮了，不必在任何地方多寫一行。

**A 與 B 不在上面。** 那兩個是到哪裡都同一個意思的手勢（啟用、離開），而提示是為了會變的東西存在的。

它由平台畫在 top layer 上，跟 bar 一樣，所以沒有任何 app 畫得出來、移得動、或問得到它在不在。
**它的代價是逐畫面算的**，見上面〈兩個浮動 widget〉：清單保留那個角落，代價是最底下那一列窄一點；
grid、設定頁與吉祥物什麼都不用讓，因為它們的內容永遠碰不到那個角落；
`frame: "bare"` 的 app 什麼都不保留，因為那種畫面上什麼都不畫。

**頁尾的一行字可以讓開它。** 欄式畫面的文字靠左，而那正好就是提示所在的位置，
所以 label 可以要求 `align = "right"`，改坐到另一端去。這個屬性就是為此而生的：
canvas 靠子節點的命名順序免費得到同樣的效果，而欄式畫面本來根本無從表達。

**app 可以自己關掉它**：manifest 裡寫 `"hints": false`。這跟 `bare` 是不同的主張，也值得單獨存在 ——
它說的是「**這裡的方向不需要解釋**」。預設是開的，而且這個方向是刻意的：
最需要提示的 app，正是作者沒想過這件事的那些，所以什麼都沒寫的 app 會拿到它。

**滑動不是新手勢。** 它就是搖桿，只是用手指做：本來往上推的地方就往上滑，左右亦然。
觸控與搖桿是同一套小詞彙的兩種說法——「點一下就是在那一列上短按 A」本來就成立——方向遵循同一條規則，而不是為手指另造一套語法。

這有一個必須說清楚的代價，因為它是**拿掉**了一樣東西：**清單不能再用手指自由捲動。**
拖曳移動的是選取，一次一列，視窗跟著選取跑，跟搖桿完全一樣。
如果讓手指去拖視窗，選取會被留在畫面外，而那會打破整個捲動模型唯一的支柱——選取帶動，視窗跟隨。

**瀏覽與選取完全可以用觸控完成，離開不行。** 手指可以翻、可以選、可以啟用、可以叫出選項——所有在畫面之內移動的事。
返回與回家沒有對應的滑動、也沒有可點的角落：B 是實體按鍵，而且會一直是。

**一次 tap 在抵達你之前，已經被過濾過什麼。** 平台幫觸控守好這些，所以你的 `on_click` 不必自己防；列在這裡是為了讓你知道你拿到的是什麼：

| 防護                                       | 對你的意義                                          |
| ------------------------------------------ | --------------------------------------------------- |
| 喚醒暗掉螢幕的那一下不做別的事             | 從口袋裡拿出來的裝置不會因此啟動你                  |
| 畫面剛建好的短時間內不吃 tap               | 換頁時仍按著的手指不會落在新畫面上                  |
| tap 必須起訖在同一個目標，而且幾乎沒有位移 | 從你那一列開始的拖曳是滑動，不是點擊                |
| 破壞性動作永遠不會只差一次點擊             | 這就是操作只存在於長按 A 之後、不出現在畫面上的原因 |

刻意**沒有慣性、沒有動量捲動**：carousel 只有幾格、grid 一次翻一頁，慣性是給長列表的解法，在這裡只會讓人滑過頭。

這條線劃在這裡是刻意的，而且它值兩次。**離開是唯一絕不能被誤觸引發的動作**——口袋、擱在玻璃上的拇指、面板讀到一滴水。
**它同時也是使用者必須永遠到得了的動作**，包括面板已經完全沒有回應的時候。實體按鍵在這兩件事上都勝過手勢，而且它是裝置上唯一如此的東西。

長按門檻是 `LONG_PRESS_MS`(600 ms)。在門檻前放開 → 短按；到門檻仍按住 → 長按，並抑制短按。

**短按 B。** 你的 `on_back` 會被呼叫，回傳值就是訊號：

- **truthy** → 你處理掉了(你往上一層目錄、清掉選取)。平台什麼都不做。
- **falsy，或根本沒有 `on_back`** → 平台動手：把你 push 在 root 之上的頁 pop 掉，否則就離開你的 app。

所以單頁 app 不用寫 `on_back`,B 照樣離開它；push 出來的對話框不用寫 `on_back`,B 照樣取消它。只有在你自己有一層要爬時才寫。要在 handler 裡直接離開，呼叫 `sys.exit()`。

```text
 貓  (home)                  ◄── 長按 B，在哪裡都一樣，永遠有效
   └─ 你進來的那一頁          ◄── 從 app root 短按 B 會回到這
        └─ app root          ◄── 短按 B 往上一層(你的 on_back)
             └─ push 的頁     ◄── 短按 B 把它 pop 掉(context menu、對話框)
```

**長按 A。**

**任何操作都不會出現在畫面上。** 不管是 Delete、Open 還是 Reset——app 不為自己做得到的事畫按鈕，平台也不畫。
操作只存在於一個地方：**它所作用的那個項目的長按之後**，在那一按之前它哪裡都不存在。

因此一個畫面顯示的永遠是「**有什麼**」，不是「**能做什麼**」，而主區域始終是內容，不會一半內容一半工具列。
這條規則約束平台的程度不亞於約束 app：launcher 的列上也不提供任何操作。

**它以一條從底部升起的列出現。** 一排兩顆，每顆是 `[icon] [名稱]`；超過兩個就往上捲，一次一排。
它就是一個兩欄的 `layout = "grid"` 清單，所以左右在同一排走、上下換排——**沿用 grid 自己的走法，不是第三套規則**。

```text
┌──────────────────────────────────────────────┐
│ [==] 87%        File Browser           12/45 │
│                                              │
│  底下的內容仍然看得見                          │
│  ╭────────────────────────────────────────╮  │
│  │  [✓] Ok            [✕] Cancel          │  │  ← 從底部升起
│  ╰────────────────────────────────────────╯  │     往上捲有更多
└──────────────────────────────────────────────┘
```

**A 執行聚焦的動作，B 取消**——那是預設，app 可以把 B 改綁到別的東西。
平台保留一道防護：**它拒絕把 B 綁到 manifest 標為破壞性的動作**。
B 在裝置的其他地方都是逃生口，而把 Delete 放在那裡的 app，會剛好在使用者想離開的那個反射動作上誤觸。

預設那一對是 `Ok` / `Cancel`。**Cancel 並不是 B 的贅字——它是給手指的**，因為返回與回家沒有觸控，觸控使用者需要一個看得見的出口。

既然是平台畫的，`on_options(self, index)` 就改成**回答哪些動作適用**，而不是自己建一頁再 push：它回傳一串 manifest 目錄裡的 action id。
這樣每個 app 的選項長得一樣、行為也一樣——**那正是系統 icon 集存在的意義**；自己畫選單的 app，遲早會畫出一個不一樣的 Delete。

## 誰擁有什麼

|                        | 平台                                        | 你                             |
| ---------------------- | ------------------------------------------- | ------------------------------ |
| **版面**               | 所有幾何、帶高、列高、捲動                  | —                              |
| **頂列**               | 電量、標頭、`N/total`                       | 標題**字串**                   |
| **內容**               | 保留區域、繪製你的樹                        | 建那棵樹                       |
| **選取**               | post `prev`/`next`/`click`、捲動跟隨        | `selected` 整數                |
| **icon**               | 解析 glyph 名與 `icon.png`、吉祥物 fallback | 挑**哪一個** icon              |
| **短按 A / 觸控**      | 偵測、post                                  | `on_click`——「啟用」是什麼意思 |
| **長按 A**             | 偵測、帶索引 post                           | `on_options`——建 menu          |
| **短按 B**             | 你不接時負責 pop 或離開                     | `on_back`——接或不接            |
| **長按 B、電源、主題** | 全部                                        | —                              |

右欄就是你的全部。不在右欄裡的，就不是你能決定的。

## Config

旋鈕放在 manifest，行為放在 Lua。

| 旋鈕                                  | 位置     | 效果                                         |
| ------------------------------------- | -------- | -------------------------------------------- |
| `name`                                | manifest | 預設標題，也是你那一頁上的名字               |
| `icon`                                | manifest | 你的身分 icon；沒有 → 吉祥物                 |
| `actions[]`                           | manifest | 動作目錄：`id`、`name`、`icon`               |
| `frame`                               | manifest | `"standard"`(預設)或 `"bare"`——畫面 1 或 2–5 |
| `counter`                             | manifest | 你不是清單的話設 `false`                     |
| `hints`                               | manifest | 設 `false` 就不在你身上畫控制提示            |
| `ui.title(s)`                         | Lua      | 執行期會變的標題                             |
| `layout`                              | Lua      | 清單上：`"grid"` 放 icon，`"mixer"` 放數值   |
| `style`                               | Lua      | 這個節點用下面哪一個角色                     |
| `align`                               | Lua      | 一行要讓開提示角落時設 `"right"`             |
| `on_click` / `on_options` / `on_back` | Lua      | 接走短按 A、長按 A、短按 B                   |

### 樣式角色

你指定角色，平台決定它長什麼樣。你的樹裡沒有字體、沒有大小、沒有顏色，理由跟沒有座標是同一個。

| 角色      | 用在                                 |
| --------- | ------------------------------------ |
| `body`    | 預設——你沒有標記的一切               |
| `title`   | 你區域裡的標題                       |
| `caption` | 答案底下的計算過程：單位、路徑、提示 |
| `primary` | 兩者之中肯定的那個                   |
| `danger`  | 不能復原的那個                       |
| `display` | **一個**大到值得獨佔畫面的數字       |

字級是平台的，不是你能設的，但值得寫下來，因為那正是「一個角色就夠了」的理由：

| 角色      | 字級 | 為什麼是這個                 |
| --------- | ---- | ---------------------------- |
| `body`    | 16   | 清單的一整列，在一臂之遙讀   |
| `title`   | 20   | 標題，比底下的列高出剛好一階 |
| `caption` | 10   | 計算過程，刻意不跟答案搶     |
| `display` | 96   | 一個獨佔面板的數字           |

`body` 與 `title` 以前是 14 與 16。**清單是這台裝置絕大多數時間所呈現的畫面**，而 14 是要湊近才讀得舒服的字級——
對一台在桌前用兩手拿著的機器來說，那是設計錯了姿勢。
一起調也是必要的：只動一個會讓標題只比它底下的列大一根頭髮，那比沒有標題更糟。
代價是每頁少幾列，而〈五種畫面〉已經說了區域要切成整數列，所以少掉的是**看得見的**，不是被切掉的。

**`display` 只吃數字**——`0-9`、冒號、句點、減號。塞字母進去會得到缺字方框，這是刻意的：它存在是為了一個已經贏得整片面板的量，而一個同時也能當「大字」用的角色會立刻變成那樣，五個文字角色就不再是設定文字的唯一途徑。

**`frame: "bare"`** 會拿掉頂列，把整片 320x240 給你——給真的是一塊畫布的 app(遊戲、時鐘面)，而不是清單。代價是失去電量、標題與計數；它換到的是**空間，不是座標**：規則 1 依然成立，你依然是建一棵沒有幾何的樹。

## 範例：File Browser

manifest 宣告身分與動作目錄：

```json
{
  "id": "filebrowser",
  "name": "File Browser",
  "entry": "main.lua",
  "icon": "icon.png",
  "permissions": ["fs.read", "fs.delete", "fs.reset"],
  "actions": [
    { "id": "open", "name": "Open", "icon": "file" },
    { "id": "rename", "name": "Rename", "icon": "edit" },
    { "id": "delete", "name": "Delete", "icon": "trash" }
  ]
}
```

Lua 決定行為。樹在檔案底部建一次，之後只改屬性(規則 2):

```lua
local rows = ui.list{ id = "rows",
  on_prev = function() move(-1) end,
  on_next = function() move(1) end,

  -- 短按 A 或觸控：觸控時 index 會來，短按 A 時沒有
  on_click = function(self, index)
    if index then B.sel = index; self.selected = index end
    local e = B.entries[B.sel]
    if not e then return end
    if e.is_dir then descend(e) else show_file(e) end
  end,

  -- 長按 A:menu 取決於項目，所以在這裡建
  on_options = function(self, index)
    local e = B.entries[index]
    if not e then return end
    local acts = e.is_dir and { "open" } or { "open", "rename", "delete" }
    ui.push(menu_for(e, acts))   -- 列是從目錄取出的 [icon] [name]
  end,
}

ui.screen{ rows,
  -- 短按 B：還有得爬就爬，爬到頂就讓平台把我們帶出去
  on_back = function()
    if B.cwd == "" then return false end   -- 在 root：平台離開這個 app
    up(); return true                      -- 爬了一層：平台什麼都不做
  end,
}
ui.title("SD:/")   -- refresh() 裡也要再呼叫，因為路徑會變
```

填列時要保留 node、只寫屬性——重用的列只是一次文字更新，它背後的 widget 從不重建：

```lua
local function fill_rows()
  local old, kids = rows.children, {}
  for i, e in ipairs(B.entries) do
    kids[i] = old[i] or ui.label{ id = "row" .. i }
    kids[i].text = e.name
    kids[i].icon = e.is_dir and "folder" or "file"   -- 規則 5
  end
  rows:set_children(kids)
  rows.selected = B.sel
end
```

## 範例：時鐘

_錶面已實作，設定頁還沒（#73）。_ 上面的 File Browser 是清單；這是同一套詞彙的另一端，而它不需要平台還沒有的任何東西。
**它是三個畫面**，而說清楚每一個是五種裡的哪一種，幾乎就把所有問題都解決了：

| 層           | 畫面   | 顯示什麼                         | 怎麼到     |
| ------------ | ------ | -------------------------------- | ---------- |
| **carousel** | 吉祥物 | `14:32`——除此之外什麼都沒有      | 從貓左右翻 |
| **錶面**     | 全螢幕 | 時間、日期、星期、這個時間哪來的 | 短按 A     |
| **設定頁**   | 設定   | 時、分、日、月、年               | 短按 A     |

短按 B 沿著同樣這三層往回走，長按 B 在任何一層都直接回到那隻貓。

**carousel 只顯示時間，到此為止。**
它是一圈 app 裡的一格，是使用者為了找別的東西而快速翻過去的其中一格；在那個速度下，它欠使用者的就是他們來拿的那一個事實。
日期、星期、對時來源用 carousel 的字級擠在一起，是「要求一件事、卻要讀四件」；把它們放到有空間的地方，它們才值得被讀。

**錶面吃掉整片面板**——`frame: "bare"`，沒有 bar、沒有計數、**沒有提示**。
那就是把它升格的理由：這是一個**被看著**的時鐘，而一個被看著的時鐘，上面不該有任何跟裝置有關的東西。

```text
┌────────────────────────────────────────────────┐
│  2026-09-10                                    │  title
│                                                │
│                   14:32                        │  display
│                                                │
│           S M T W [T] F S         NTP 14:30    │  一條 strip，與 body
└────────────────────────────────────────────────┘
```

三層資訊、三種字級，而且**沒有一層需要讀第二眼**：日期在角落等想找它的人，時間在正中央撞上視線，星期是七個字母，只有今天是亮的。
那排字母的價值在於它根本不需要被讀——七個位置，亮的那個就是答案。

```lua
local letters = {}
for i, d in ipairs{ "S", "M", "T", "W", "T", "F", "S" } do
  letters[i] = ui.label{ id = "day" .. i, text = d, style = "caption" }
end
local week = ui.list{ id = "week", layout = "row", align = "center" }

ui.screen{
  ui.label{ id = "date", text = "2026-09-10", style = "title", align = "left" },
  ui.label{ id = "time", text = "14:32",      style = "display" },
  week,
  ui.label{ id = "src",  text = "NTP 14:30",  style = "body" },
}
week:set_children(letters)      -- 今天那一個改成 body
```

**七個節點，因為一個節點只帶一個角色。**
「這七個字母，今天那個比其他都亮」是七個角色，否則就什麼都不是——而那正是 `layout = "row"` 存在的理由。
strip 讓它們**共用一個字級**、只在墨色上不同，所以這條線**不會在午夜從眼前跳一下**——而那正是沒有人在看它的那一刻。

**命名順序就是版面，而 `align` 負責解開順序解不開的平手。**
日期命名在中心之前，所以它是上面那條線；星期條與來源行命名在它之後，所以它們是下面那條，第一個靠左、最後一個靠右——
這就是為什麼來源行會落在右下角，而沒有任何一句話說了座標。

真的說了 `align` 的那兩個，正是順序判不出來的那兩個。
**一行字如果那條線上只有它自己，它同時是第一個也是最後一個**，所以「置中」只是一個猜測：`align = "left"` 才是把日期放進角落的那句話。
而星期條是下面那條線的第一個，順序會把它靠左——但一個「被看」而不是「被讀」的東西，該在正中間。

**「這個時間哪來的」只住在這裡。**
不在 carousel，在那裡它會佔掉螢幕上四分之三的東西；也不在設定頁，
在那裡放一行「網路已經回答過這件事了」，等於在一個正要自己回答這件事的人肩膀後面唸稿。
在錶面上，它是「我懷疑這個時間」唯一能被解決的地方——而那也正是任何人會問的時候。

**沒對過時的時鐘什麼都不說**，那一行單純不存在。
**沒設定過的時鐘會說出來。** RTC 剛上電時從未被設定，而一個自信的 1 月 1 日 `00:00` 比承認不知道更糟：它會被相信。
所以同一個錶面用同樣的字級、同樣的位置顯示 `--:--`，星期條**一個都不亮**——七個位置都在，但沒有一個是今天——底下一行 `caption` 說明按 A。

**設定它用的就是「設定」畫面**，原封不動——那正是把五種畫面命名出來的全部理由：

```text
      14      32       9      Sep     2026
     ╔═══╗  ┌───┐   ┌───┐   ┌───┐   ┌───┐
     ║███║  │███│   │███│   │███│   │███│
     ╚═══╝  └───┘   └───┘   └───┘   └───┘
     Hour    Min     Day    Month    Year
```

左右選一欄、A 聚焦它、上下改值、A 寫入、B 放回去——就是偏好頁的那一頁，只是裡面換成時鐘的數字；
一個改過一次亮度的使用者，已經學會這一頁了。
**時和分會繞回**，而偏好設定是夾住兩端的：23 之後是 00，因為時鐘是一個環，亮度不是。

**從錶面進設定頁用的是短按 A，而規則說那裡應該是長按 A。**
規則 7 把操作放在長按 A 之後，是因為有一個「選取項目」讓它去作用；
錶面沒有選取，短按 A 也不可能有別的意思，所以時鐘把它花在自己唯一的那個操作上。
長按 A 做同一件事，所以從別的 app 過來的使用者會在他們預期的地方找到它。
兩種按法都**沒有在畫面上畫出任何東西**，而那才是規則 7 真正約束的部分。

## 首頁，以及你的 app 在哪裡被找到

_已設計，尚未實作。_

**首頁就是那隻貓。** landing page 不是 app、也不是選單；它是每一個手勢都到得了的那一頁，也是長按 B 永遠落地的地方。
它就是主區域的「一個 icon」形狀。

它兩側各有一層，而**這兩層不是同一件事的兩種做法**——一層是「**你的東西**」，另一層是「**全部東西**」。

**carousel 是精選層。** 從首頁出發，上是你所有的 app，下是系統，左右翻過你留下來的那幾個。一次一個，大。

```text
                        ▲  全部 app — 3x2 grid
                        │
   釘選  ◄──────────  [貓]  ──────────►  釘選
                        │
                        ▼  這台裝置是什麼
```

上與下不是同一件事的兩個方向：**上是你的，下是裝置的**。
時鐘不是一個方向，它是一個 app，跟其他 app 一樣釘在 carousel 上，而且你寫的東西可以取代它。
為了一個畫面養一整套「平面」機制，換到的只有「沒有人能取代它」——那不值得。

四個方向在這裡代表四件不同的事，這正是它是 home section 而不是一排橫著放的清單的原因。
**在 carousel 的任何一格往下都到得了裝置頁**，不必先回到中間那隻貓才能改亮度。

**下是裝置，而裝置先回答、再提供。** 往下打開的是一頁事實——版本、晶片、還剩多少、時鐘說現在幾點、匯流排上誰在應答——底下擺兩塊 tile：

```text
  ┌──────────── Device ────────────┐
  │ catnip v0.3.1                  │   事實可以捲：上下在裡面移動，
  │ built 2026-09-09               │   而在一條事實上按 A 什麼都不會
  │ clock 2026-09-09 14:03         │   發生，因為事實不是選擇
  │ battery 82%   card none        │
  ├────────────────────────────────┤
  │      ⚙            ⚠           │   左右選 tile，A 進去
  │  Preference   Diagnostic       │
  └────────────────────────────────┘
```

這個順序就是設計本身：「這是什麼東西」要在「改它」之前被回答，更在「搖桿是不是壞了」之前。
底下這兩頁都是平台自己的——都不在 grid 裡、都不能被釘選，你寫的東西也取代不了它們。

**偏好頁**用的是上面那個「數值」形狀。A 存檔離開、B 放棄離開，兩者都回到這一頁而不是那隻貓；長按 B 才是一路回家的那個，跟它在別處的意思一樣。

**診斷頁**吃掉整片面板——它就是在測那片面板，很難跟人共用——而**長按 B 把面板還回來**。
它以前是用重開機代替離開的：說它是出口也對，畢竟重開完就會看到那隻貓；但對手上拿著這台機器的人來說，那不是出口。

### 設定一個值

每一個設定值的頁面都用同一套操作，不管它是平台的還是你的——偏好頁和時鐘的設定頁**是同一頁**。
它就是〈五種畫面〉裡的**設定**畫面：數值形狀、像 grid 一樣分頁，而且有**兩層**。

|            | 未聚焦——你在頁上   | 聚焦中——你在某一欄上       |
| ---------- | ------------------ | -------------------------- |
| 上 / 下    | 上一頁 / 下一頁    | 改這個值；按住連發         |
| 左 / 右    | 在這一頁的欄之間走 | 沒有作用                   |
| **短按 A** | **聚焦那一欄**     | **寫入，並放開**           |
| **短按 B** | **離開這一頁**     | **把這一欄放回去，並放開** |
| **雙擊 A** | **全部寫入並離開** | **全部寫入並離開**         |
| 長按 B     | 回家               | 回家                       |
| 點某一欄   | 聚焦它             | 設到手指所在的位置         |

**聚焦不是一個模式，它是「選取／取消」往下一層。**
A 是選取：在頁上，那代表這一欄；在欄上，那代表這個值。
B 是取消：在欄上，那代表你正在改的這個值；在頁上，那代表這一頁。
四個固定手勢在這裡的意思跟在別處**完全一樣**，沒有任何新東西要學——
這也是為什麼上下在兩層都是亮的，而提示不必說出任何不同的話：它們在兩層都有作用，而那就是提示唯一宣稱的事。

**左右在聚焦中是死的**，而這正是這一層唯一值得花一個手勢換的東西。
改一個值是整台裝置裡唯一一個「往旁邊推一下、結果落在鄰居身上」會**默默出錯**的地方——
你會動到一個你根本沒在看的數字。未聚焦的時候，同一下推是免費的。

**值在你調整的當下就生效**，因為一個你在選的時候看不到的值，是一個你會選兩次的值。
A 寫入的就是你一直在看的那個；B 放回去的是你聚焦那一刻它的值。
所以在未聚焦的狀態離開這一頁，不會丟掉任何「還沒被放回去」的東西。

**雙擊 A 全部寫入並離開，而且只有這裡有。**
裝置上其他每一個畫面的 A 都只有一個意思，只有這一頁有兩個，所以這個形狀才有位置擺；
而且這也是唯一一頁「**而且我弄完了**」是一句獨立的話的地方——因為只有這裡你可能改了三個東西，
否則就得一路走回去經過那三個。手指說同一句話的方式一樣：點兩下。

點一下和按 A 是同一個事件、不是同一件事：手指有一個「它落在哪一欄」可以說，所以它聚焦那一欄；
按鍵沒有，所以它只能是在講 ring 現在停在哪一欄。
**「往下就是設定」唯一的例外是 grid**：在 grid 裡往下是把選取往前推，推完這一頁就在它底下翻頁，
因為讓一份長清單在被走過時掉出自己，代價遠大於它省下的那一個手勢。

**grid 是全部。** 在 carousel 的任何一格往上，打開的是 **Icon grid** 畫面：3x2、卡上每一個 app，超過六個就**翻頁**。
找到第十二個 app 是兩頁，而不是十二次翻——**那是 carousel 單獨做不到的事**。

聚焦的 icon 加外框，而**它的名字就顯示在標頭的 `[TITLE]`**——所以 icon 底下不需要標籤，名字也還在。

**釘選讓 app 在兩層之間移動。** 在 grid 的 icon 上**長按 A 就是那個開關本身**——釘上或取消釘選，
而不是跳出一個只有一個選項的選單，因為那等於花一次按鍵換一個沒有選擇的選擇。

這個集合存在 config 裡，跟其他設定放在一起、走同一條路：**先寫 NVS，有卡片再鏡射過去**。
它記錄的是**被排除在 ring 之外**的 app，而不是留在上面的那些——所以預設值（空集合）就是「全部都釘選」，
那正是一台還沒有任何歷史的機器該顯示的樣子；取消釘選才是使用者做的例外，而例外才是值得存下來的東西。

**短按 B 回到你進來的地方**——從 app 回到啟動它的那一頁或那一格，從平面回到首頁。**長按 B 則是不論多深，直接回到貓。**
這正是讓兩者在 app root 不致變成同一件事的地方。

**這對你的 app 的要求。** 你的 `icon.png` 是你在 grid 與 carousel 裡的臉，而且是在真的看得清楚的尺寸上。
請附一個；沒有的會退回吉祥物，那是誠實的，但也代表所有沒 icon 的 app 長得一模一樣。
manifest 裡的 `name` 是你被聚焦時標頭顯示的字，它會被單獨閱讀而不是排在一整欄裡——要讓它站得住。

觸控不需要額外設計就到得了這裡的大部分：滑動翻 carousel、翻 grid 的頁，就跟推搖桿一樣；點一下會把 ring 移到那個 icon 上。

**但在 grid 裡，點一下就只是選取，到此為止。** 啟動是短按 A、釘選是長按 A，手指兩件都做不到。
grid 正是誤觸代價最大的地方——啟動錯的 app、或把你依賴的那個取消釘選——而擋住它的方式不是跳確認視窗，
而是**觸控根本碰不到那個動作**。點一下仍然會移動 ring，所以手指是「快速到達」的方式，搖桿才是「確認執行」的那一下。

## 這份規格要求了什麼，以及每一項落在哪裡

〈五種畫面〉原本描述的是這台裝置**要去哪裡**。它要求的七項全部做完了，這張表留下來是為了記錄**每一項落在哪裡**，
不再是一張欠款清單：

| #   | 要改什麼                                                        | 在哪裡                                  |
| --- | --------------------------------------------------------------- | --------------------------------------- |
| 1   | ~~grid 改成分頁而不是捲動；固定三欄~~ — 已完成                  | `catnip_app_grid.c`、grid layout        |
| 2   | ~~清單區域高度切成整數列~~ — 已完成                             | frame 的區域計算                        |
| 3   | ~~`body` 14 → 16、`title` 16 → 20~~ — 已完成                    | `lvgl_backend.cpp` 的 role → font       |
| 4   | ~~設定畫面：分頁、兩層聚焦、雙擊 A~~ — 已完成                   | 偏好頁與 input pass                     |
| 5   | ~~提示依「內容碰得到角落嗎」保留、動作列升起時騎上去~~ — 已完成 | frame                                   |
| 6   | ~~動作列：兩 icon 與三 icon 兩種形狀~~ — 已完成                 | `catnip_bar.c`、`frame.cpp`、input pass |
| 7   | ~~時鐘變成三個畫面~~ — 已完成                                   | `apps/clock/main.lua`、`manifest.json`  |

**兩個 app 改了，而兩個都不是被迫的。**
時鐘變成三個畫面，是因為它**本來就是**三個畫面；File Browser 改了，是因為它自己在建選項選單，而動作列把那件事接走了——
它少掉六十行、多了一份 manifest 目錄。WiFi Prober **一行都沒動**：1–6 每一項都是平台在這道接縫上的那一側，
而這正是這道接縫存在的理由。一個「為了讓 `body` 變大而必須被編輯」的 app，等於證明角色從來沒有生效過。

1–6 每一項都能在既有的 host 層測：`test/native` 測 input pass 與 page machine 決定了什麼、
`test/layout` 測 LVGL 實際擺在哪裡，截圖也是從後者出來的。
第 7 項的測法跟 `test_filebrowser.c` 測 browser 一樣：載入 app 的 Lua、驅動它、把樹讀回來。
